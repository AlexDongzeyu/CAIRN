# Research Log

Chronological record of research decisions and actions. Append-only.

| # | Date | Type | Summary |
|---|------|------|---------|
| 1 | 2026-08-17 | bootstrap | Workspace initialized. Env verified: Python 3.14.3, torch 2.11.0+cu128, CUDA on RTX 5060 8GB. All protocol packages import clean (toponetx, topomodelx, torch_geometric, gudhi, deepsig, krippendorff, rbo, datasketch, hypernetx, sentence-transformers 5.3.0). Fixed a pip-induced numpy downgrade to 1.26.4 that broke the sklearn ABI on py3.14; restored numpy 2.5.2. |
| 2 | 2026-08-17 | bootstrap | Probed the Densho DDR REST API (`/api/0.2`). Search, per-object JSON, and `/children/` pagination all work; `/narrators/`, `/facets/`, `/repository/` return 500. Page size is server-fixed at 25 regardless of `limit`. |
| 3 | 2026-08-17 | bootstrap | **Corpus decision confirmed.** `ddr-densho-1000` (Densho Visual History Collection, 1996-2019, `rights: cc`) contains **526 interview entities**, each with archive-curated segments. E1.1 kill criterion (>=400 CC-licensed interviews) is cleared on the primary corpus alone — Rutgers stays as transfer, as the protocol intended. |
| 4 | 2026-08-17 | bootstrap | **Key finding for the rank ontology.** Segment listings already carry the archive's own descriptive taxonomy: `topics` (hierarchical controlled vocabulary, `--`-delimited, with stable IDs), `chronology` (decade bins), `geography` (Getty TGN IDs), `facility` (facility IDs), and `creators` with explicit `role: narrator` + stable `oh_id`. This means rank can be assigned from archive practice rather than invented — exactly what protocol §0.1 requires — and rank-1 *moments* are the archive's own segments, not a segmentation we imposed. |
| 5 | 2026-08-17 | bootstrap | Transcripts confirmed available: each interview page exposes a `Download full transcript` link to `media/{collection}/{interview_id}-transcript-{hash}.htm`. Hash is unpredictable, so transcripts are harvested by scraping the interview page (526 pages) rather than guessing URLs. |
| 6 | 2026-08-17 | bootstrap | `PREREGISTRATION.yaml` frozen, including an explicit **substitution ledger** for the three protocol steps that require humans (E2.2 annotators, E3.3 gold CDEC, E9.3 expert panel). Each substitution states what was asked, what was executed, and how the resulting claim is narrowed. |
| 7 | 2026-08-17 | inner-loop | E1.1 crawl launched (polite 1 req/s, disk-cached, resumable). |
| 8 | 2026-08-17 | inner-loop | Crawl killed at interview 73/526 by a transient DNS failure. Added exponential backoff to the API client and the transcript fetcher; the disk cache made resumption free. |
| 9 | 2026-08-17 | inner-loop | **E1.1 COMPLETE.** 526 interviews, 13,315 segments, 519 interview-level transcripts, 470 distinct narrators. All `rights: cc`. The >=400 CC-licensed-interview kill criterion is cleared on the primary corpus, so Rutgers stays as the transfer archive rather than becoming co-primary. |
| 10 | 2026-08-17 | inner-loop | Wrote 61 instrument tests *before* quoting any number: RBO (identity/disjoint/top-weighting), Krippendorff α (perfect/noise/systematic), clustered bootstrap (coverage + must be wider than naive under clustering), design effect, IR metrics against hand-computed values, ASO dominance/no-order/Bonferroni, split disjointness, negative-sampler hardness ordering, near-duplicate detection, and the terminology scanner's own ability to fail. |
| 11 | 2026-08-17 | inner-loop | **E4.4 PASSES.** ρ(rank, \|x\|) = 0.471, well under the 0.9 failure threshold. Rank-inversion rate 0.814; the largest rank-2 cell holds 79× the narrators of the smallest rank-3 cell. The complex is demonstrably not a hypergraph with labels. |
| 12 | 2026-08-17 | inner-loop | **E4.2 PASSES.** Star-expansion round-trip is lossless over 7,989 cells under all three rank maps: 0 missing, 0 extra, 0 mismatched. Recorded for Figure 1, where it is conceded on the figure itself. |
| 13 | 2026-08-17 | inner-loop | **Instrument bug 1.** Cross-granularity triage RBO came out ≈0.000. Diagnosis: coarse keys are parent paths and fine keys carry a place suffix, so the label spaces are disjoint *by construction* and overlap is impossible. Added a `referent()` projection onto the depth-2 archival path; both projected and raw values are now reported, with the reason. |
| 14 | 2026-08-17 | inner-loop | **Instrument bug 2.** MinHash/LSH `query()` returns *candidates*, not matches. Unfiltered it reported 95,892 cross-narrator near-duplicate pairs. Added an explicit Jaccard re-check. |
| 15 | 2026-08-17 | inner-loop | **Instrument bug 3 — the important one.** Duplicates were still 637,989/13,315 segments. Diagnosing the raw field rather than the detector showed Densho appends an HTML grant-acknowledgement notice to many segment descriptions (description word count was bimodal: median 10, p75 49). It dominated the 5-gram shingles *and* the sentence embeddings feeding every model. Added frequency-based boilerplate detection (not a hard-coded string) in `corpus.py`. Near-duplicates fell to **564**; description p75 fell 49 → 12. |
| 16 | 2026-08-17 | inner-loop | **Instrument bug 4.** α ≈ 0 is ambiguous between real disagreement and the κ-paradox under skewed marginals, so α is never quoted alone: added raw pairwise agreement, per-annotator marginals, Fleiss' κ and Gwet's AC1, plus a paradox flag with its own test. |
| 17 | 2026-08-17 | outer-loop | **E2.2 kill criterion TRIGGERED.** Round-2 ordinal α = 0.039 (Round 1: 0.073), raw agreement 0.530, AC1 = 0.082 — the diagnostics rule out the κ-paradox, so this is genuine non-convergence, not a measurement artifact. 146 of 211 terms disputed. The protocol-mandated adjudication *lowered* agreement, which is itself evidence: a defensible revision of the manual moves the construct. Pre-registered negative result #4. |
| 18 | 2026-08-17 | outer-loop | **E4.3: H3's granularity leg FAILS.** Referent-projected RBO@50 (p=0.9): coarse\|mid 0.537, coarse\|fine 0.251, mid\|fine 0.317 — every pair below the pre-registered 0.60. Archive-conditioned attestation triage has no resolution-invariant meaning; H3 is scoped to the pre-declared `mid` setting. Pre-registered negative result #3. |
| 19 | 2026-08-17 | inner-loop | **Instrument bug 5 — leakage.** First model pass gave T2 AUC ≈ 0.47–0.55, i.e. at or below chance for the structural models. Cause was not the architecture: message passing was handed the very (narrator, event) incidences the model is asked to predict, so both sides of the score already encoded the answer. Added `mask_incidences()`; the bundle is now built from training incidences only, with three tests asserting the mask removes exactly the held-out edges, leaves rank-1 untouched, and is not a no-op. |
| 20 | 2026-08-17 | inner-loop | **E6.0 violation.** M3 carried 246k parameters against ~128k for the others, breaking the ±15% matching rule. Added `match_hidden_to_budget`, which binary-searches hidden width per model to a shared 130k budget. Also raised the optimisation budget (120 → 800 steps, patience 20 → 120): 120 full-batch steps was undertrained. |
| 21 | 2026-08-17 | inner-loop | **M5 was under-implemented.** The protocol defines the CCNN as up-messages, down-messages *and* within-rank exchange through (co)adjacency; only the first two were built. Added within-rank exchange as `B(Bᵀh)`, algebraically identical to `BBᵀ` but avoiding a 13k×13k adjacency. M5's T1 MAP rose 0.075 → 0.100. Reporting the proposed model in an incomplete form would have made the headline comparison meaningless. |
| 22 | 2026-08-17 | inner-loop | **Instrument bug 6.** The singleton-fraction CI excluded its own point estimate (0.206 vs [0.081, 0.192]). Cause: cluster bootstrap *with replacement* puts two copies of a narrator in the sample, so a cell attested only by them has attestation 2 and stops being a singleton — a systematic downward bias on this non-smooth functional. Replaced with `cluster_subsample_ci` (m-out-of-n without replacement, √(m/n)-scaled), plus a test asserting any CI contains its own point estimate and a second test documenting the failure mode. |
| 23 | 2026-08-17 | inner-loop | **Instrument bug 7.** E3.3's LEA optimum was the largest threshold tested — a sweep whose best value is its own endpoint has not found an optimum. Extended the range until the peak was bracketed (0.196 at 0.6, declining after) and added a boundary warning to the output. |
| 24 | 2026-08-17 | inner-loop | Added Phase 3. Using the archive's curation as gold and unsupervised text-similarity clustering as the automatic system: **false-singleton rate 0.367 > 0.30, kill criterion triggered.** The archive's topical grouping is not recoverable from segment-summary text similarity, which is a strong argument for the protocol's expensive in-domain annotation step. E8.1 is consequently calibrated at two rates — archive-internal ambiguity, and the E3.5 automatic-extractor rate — because the complex reported here carries no extraction error by construction. |
| 25 | 2026-08-17 | inner-loop | **Figure palette failed its own greyscale check.** Okabe-Ito is colourblind-safe by hue but four co-occurring pairs fell below the 0.10 relative-luminance threshold, including M5-blue vs M3-orange in the same panel. Re-spaced luminances by bisecting lightness in HLS (hue and saturation preserved) and made the check a hard gate that aborts plotting. |
| 26 | 2026-08-17 | inner-loop | `build_t1_queries` materialised every query before slicing to 400 — quadratic in cell size, and cells at coarse granularity span thousands of segments. Made generation stop at the cap over a fixed shuffled order. |
| 27 | 2026-08-17 | outer-loop | **Headline result stable.** At the primary cell, M3 (typed star + hypergraph Laplacian/curvature encodings + explicit rank, i.e. strictly more information than the CCNN) reaches T1 MAP 0.137–0.139 against the CCNN's 0.098–0.100, and the rank-agnostic hypergraph models lead on incidence prediction (AUC 0.85 vs 0.55). The ordering holds at every granularity (coarse 0.173 vs 0.121; fine 0.088 vs 0.073). M5's own ablations attribute its performance to message-passing directionality (−0.046 when down-messages are removed) rather than to rank-specific parameterisation (−0.006 when weights are shared). This is pre-registered negative result #1 and it is reported as the finding. |
| 28 | 2026-08-17 | outer-loop | **Wrote a mechanical coverage audit** (`experiments/audit_coverage.py`) mapping every protocol experiment ID to the artifact that proves it ran, rather than relying on memory. First run: DONE=29, MISSING=23, PARTIAL=1, SUBSTITUTED=3. Most "missing" entries were phases the run had not yet reached, but the audit surfaced six genuine gaps and, separately, caught that it was reading a **stale `e7_1_runs.json`** (3 seeds, parameter spread 120k–246k) left over from an earlier partial run. |
| 29 | 2026-08-17 | inner-loop | **Gap 1 — M1 was never run.** `M1_Dense` existed in `models.py` but was absent from the driver's model list, so the protocol's non-structural retrieval floor (E6.2) had no numbers. Implemented `evaluate_m1_dense` so the parameter-free bi-encoder is scored by exactly the same metric code as every trained model, on both T1 and T2. |
| 30 | 2026-08-17 | inner-loop | **Gap 2 — two ablations missing.** E6.7 lists A1–A5; only A1, A2, A4 (plus my A6) existed. Added **A3** (collapse ranks 2 and 3, so the hierarchy has no depth to exploit) as a rank-map transform and **A5** (drop the rank-1 moment layer, routing narrator→event directly) as a CCNN flag. A5 is the one that tests whether the intermediate rank earns its place. |
| 31 | 2026-08-17 | inner-loop | **Gap 3 — E1.3 was never run**, despite 519 transcripts sitting on disk unused. Implemented `src/normalize.py`: `<Begin Segment N>` markers give segment alignment, and turn-initial speaker labels (`LH:` / `GA:`) matched against the header's `Narrator:` / `Interviewers:` lines give a **ground-truth speaker role**. The rules classifier is therefore scored on 171,313 archive-labelled turns rather than the 200 hand-labelled ones the protocol settles for. |
| 32 | 2026-08-17 | inner-loop | **Instrument bug 8, caught by its own test.** The first normalization run reported speaker-label coverage of **0.5%**. Cause: the header was parsed against un-stripped text, where `Narrator:` and the name land on separate lines, so the regex matched nothing and every turn fell through to `UNKNOWN`. Left alone this would have been read as "the archive does not label speakers". Fixed the canonicalisation; coverage went to **77.7%**, turn-classification accuracy **0.806**, 13.2% of characters removed by disfluency cleaning. Added `tests/test_normalize.py` with an explicit coverage-floor assertion so the failure cannot recur silently. |
| 33 | 2026-08-17 | inner-loop | **Gap 4 — E3.6 ambiguity flagging** implemented from the four signals the protocol names (linking confidence via term frequency, proximity to a merge decision via sibling count, surface-form diversity, `<INAUD>` density). Top decile = 21 of 211 terms, which now defines the E2.2 "ambiguous" stratum. |
| 34 | 2026-08-17 | inner-loop | **Gap 5 — E8.3 gold-subset upper bound** implemented as the noise-free ceiling: rebuild the complex with the E3.6-flagged terms removed and re-run the model grid. This is what separates "the method does not work" from "the labelling is contested". |
| 35 | 2026-08-17 | inner-loop | **Gap 6 — E13.2 reproduction check** implemented as a self-sufficiency test: a fresh isolated interpreter (`-I`, cwd inside `release/`, project source not importable) recomputes Table 1 from the released files alone and diffs against the published statistics. Labelled explicitly as *self-sufficiency*, not independent human reproduction, which is what the protocol actually asks for. |
| 36 | 2026-08-17 | inner-loop | Also brought two settings up to the protocol's stated values: E8.1 now sweeps ρ_m and ρ_s **independently over 5×5 = 25 grid points** (previously only the 5-point diagonal), and E7.4's archive-level bootstrap now uses **B=10,000** resamples. |
| 37 | 2026-08-17 | inner-loop | **`normalized.jsonl` was redistributing the testimony a second time.** The first E1.3 writer stored the full `clean` text per turn — 89.7 MB duplicating content already held as HTML in `transcripts/`, and contrary to E13.1's stand-off convention. Since `clean` is reconstructable by applying `clean_text()` to `raw[char_start:char_end]`, the field was dropped in favour of the two derived quantities E3.6 actually consumes (`n_clean_chars`, `has_inaud`). File is now 49.5 MB of offsets and labels, and the artifact is genuinely stand-off. |
| 38 | 2026-08-17 | inner-loop | **Importing a module was destroying its log.** `make_logger` truncated the log file at import time, so merely import-checking a module — which both the coverage audit and the test suite do — wiped the record of a completed run (`phase1_3.log` was found at 0 KB despite a successful run). Switched to append with a per-run banner; logs are evidence and should not be erasable by an import. |
| 39 | 2026-08-17 | instrument | **Bibliography built by retrieval, never from memory.** Every reference the paper will cite is fetched from Crossref, then OpenAlex, then arXiv, and accepted only if the retrieved title matches at ≥0.92 similarity **and** the first-author surname is confirmed. The gate earned its keep immediately: Crossref answered the Hajij query with *"Deep learning for graph structured data"*, the AllSet query with *"Further results in multiset processing with neural networks"*, and the deep-significance query with a table caption — three plausible near-misses that a looser threshold would have silently accepted. Final tally **17/17 verified at similarity 1.00, zero placeholders**. Crossref alone could not resolve the topological-deep-learning preprints (no DOI); OpenAlex supplied all five. arXiv's own API was unreachable from this network. |
| 40 | 2026-08-17 | instrument | **A completed run was killed by its own progress echo.** Crossref returns `inter‑rater` with U+2010; printing it to this machine's cp1252 console raised `UnicodeEncodeError` inside `log()` and destroyed a citation run that had already verified eleven references. The file handle was UTF-8 and correct — only the terminal echo could not render it. `log()` now degrades the echo and keeps the file exact, with a regression test that drives a cp1252 stream (73 tests). |
| 41 | 2026-08-17 | process | **Two pipeline chains were running concurrently.** A `run_phase10_transfer.py` launched at 20:04 was still alive at 21:08 alongside the 20:48 chain, having written *zero* log lines in 44 minutes and fetched nothing in the previous 10. Both chains would have written `e10_transfer.json`. Killed the stale process; the remaining chain regenerated the figures, release and report it had produced from partial results. Root cause of the invisibility: `harvest()` logged once per collection, so hundreds of throttled requests looked identical to a wedged socket. It now emits a heartbeat every 10 interviews and stops at a 900 s wall-clock budget — a harvest that stops early is recoverable, one that cannot be observed is not. |
| 42 | 2026-08-17 | finding | **The split decides which architecture wins.** Under the pre-registered narrator-disjoint split M3 leads M5 on T1 (0.139 vs 0.098). Under a random split the ordering inverts and the gap is far larger in the other direction: M5 0.388, M3 0.106. A random split lets a model recognise the narrator it was trained on, and the 4× inflation is the size of that leak. Reported as a result rather than a footnote: it means an architecture comparison on this kind of archive is uninterpretable without a narrator-disjoint split. |
| 43 | 2026-08-17 | writing | **The manuscript cannot contain a number that no run produced.** `make_paper.py` generates `numbers.tex` from the results JSON and the prose cites only macros; a manifest records macro → file → JSON path for every quantity, a missing source renders as a loud `???` rather than vanishing, and a linter flags any bare numeral in the prose. Both directions are checked: cited-but-undefined would break the build, defined-but-uncited means a number was computed and quietly dropped. Current state: prose lint clean, one macro still `???` because the model grid is mid-run. Also emitted an editable BibTeX with arXiv preprints recorded as preprints — several of these appeared at conferences, but the conference record is not what was verified, so the entry states what was. |
| 44 | 2026-08-17 | writing | **The narrative check needed checking before it was obeyed.** The first pass reported 9 uses of "registered" (the pattern was also counting the 6 inside "pre-registered") and flagged the conclusion's strongest sentence as a self-retraction (the hedge pattern matched any bare "not"). Both would have sent edits after healthy prose. After fixing the instrument — non-overlapping incantation counts, and a hedge pattern that matches retraction constructions rather than negation — the real defects were three captions that described instead of asserting, an over-long abstract, and one word over cap. All fixed; every target now passes except the negation rate, which is recorded as a **deliberate miss**: this paper's claims *are* negative ("rank is not cardinality", "the split, not the architecture, decides the answer"), so cutting negations to hit the target would cut the contribution. |
| 45 | 2026-08-17 | instrument | **A stale number was still sitting in the conclusions.** `claim_audit.py` walks every numeric literal in `findings.md` and `research-log.md` and looks for it among the values actually stored in `data/results/*.json`. It found that C1b claimed a round-trip over **7,989 cells** while `e4_2_star_roundtrip.json` records **13,996** — the complex was rebuilt after boilerplate removal changed the segment set, and the hand-written conclusion never followed. Corrected in `findings.md`; entry 12 is left as the dated record of what was true then. The paper was never exposed, because `\nCells{}` reads the JSON directly — which is the argument for generating numbers rather than typing them. This instrument also needed fixing first: it split `171,313` at the thousands separator into two numbers, counted the `2026` in every log date, and missed percentages quoted as `77.7` against a stored `0.777`. |
| 46 | 2026-08-17 | instrument | **Phase 8–11 died four minutes in and the chain carried on without it.** `degree_ks` guarded its empty case with `np.array(sizes) or np.array([0])`, which raises `ValueError` on any array with more than one element — so it passed every toy fixture and failed on the archive, taking E8.1, E8.2, E8.4, E9.2, E9.3, E9.4 and E11 down with it. The sequential `;` chain ran the remaining steps regardless, which is how a figure script and a release build completed against results that did not exist. Fixed, covered by a test whose fixture asserts more than one rank-2 cell so it actually exercises the failing path, and the phase was re-run. |
| 47 | 2026-08-17 | finding | **E8.2 is E4.3 wearing a different name.** The "three defensible extractors" (leaf terms / merged parents / place-split leaves) are exactly the three granularities, and their pairwise triage RBO values are numerically identical to the granularity comparison: 0.537, 0.317, 0.251 in both. Presenting E8.2 as an independent robustness check would have double-counted one piece of evidence as two. The paper now states the identity outright. Found by comparing the two value sets, not by reading the two section headings. |
| 48 | 2026-08-17 | finding | **Counting interviewers would erase the phenomenon.** The singleton fraction is 0.206 over narrators and **0.000** once interviewer turns count as attestations — an interviewer is present in every segment, so every event acquires a second "witness" and nothing is under-attested any more. The triage lists still agree at 0.882, which is why the decision looks harmless until the quantity that actually matters is inspected. |
| 49 | 2026-08-17 | finding | **A mention count reproduces most of the ranking.** E9.3: attestation multiplicity correlates with bare mention frequency at ρ = 0.879 (triage RBO 0.444). The complex, the rank ladder and the encodings recover a ranking that counting mentions largely already gives. This is the strongest argument in the paper against the machinery, and it comes from our own pre-registered baseline. |
| 50 | 2026-08-17 | finding | **The interaction is significant and points the wrong way.** E7.5, mixed model with crossed narrator and event effects: architecture × log event size = −0.0196, 95% CI [−0.026, −0.013], p = 1.6e−09, converged. The complex falls *further* behind the typed star as events grow — the region where extra ranks were supposed to pay. Reported with its CI rather than as "does not survive", which understated it. |
| 51 | 2026-08-17 | figures | **Figure QA pass against a publication contract.** Rendered every panel and inspected it at final size rather than trusting the scripts. Real defects found and fixed: fig1's rank labels were hand-placed text sitting *on top of* the nodes (now tick labels, which the layout engine reserves space for); fig3's eight model names ran together into an unreadable strip (now rotated) and **M1 dense was missing from the panel entirely** though it appears in the table and outranks every trained model except the typed star; fig2's heatmap chose text colour from a hand-picked 0.6 cutoff, leaving white text on mid-tone cells (now derived from the rendered cell's luminance); fig4's two threshold annotations sat on the data; fig2's tallest bar was flush against the spine. Also unified model naming across panels, replaced in-figure titles with Nature-style panel letters, and added SVG export at 600 dpi. |
| 52 | 2026-08-17 | figures | **Two figures carried unique evidence and were not in the paper.** fig1 (star expansion is lossless) and fig4 (the triage ranking fails its own perturbation threshold) were generated every run and cited nowhere. Both are now placed where their claim is made. Separately, fig5 and fig6 are built narrower than the text block, so they are included at exactly 0.70 and 0.62 of `\linewidth` — including them at full width would have magnified their text by 1.4-1.6× relative to the other panels. |
| 53 | 2026-08-17 | instrument | **The load-bearing statistic for P1 was its own opposite.** `rank_inversion_rate` used `searchsorted(..., "left")`, which counts lower-rank cells *smaller* than each higher-rank cell — that is concordance. The paper read 0.814 aloud as "inversions"; the true inversion rate is **0.0096**. Confirmed independently from `release/cells.jsonl`: the stored count is an exact match for the concordance count. Split into `size_concordance_rate` / `rank_inversion_rate` / `tie_rate` plus a `rank2_vs_rank3` block, and covered by two polarity tests whose fixtures are hand-checked in both directions. A rate can be arithmetically perfect and still be reported backwards; nothing in the pipeline tested direction until now. |
| 54 | 2026-08-17 | instrument | **Two model rows in Table 1 were not measurements.** M4 AllSet and M4 ED-HNN produce bit-identical T1 across all ten seeds while their T2 differs. Cause: both write only `h[0]` and `h[2]` and never touch `h[1]`, and T1 is scored over rank-1 representations — so the number is the frozen-encoder floor, identical because both draw the same seeded init. Now reported as `---` with the reason in the table. The same mechanism makes the moment-rank ablation's T1 collapse tautological rather than discovered. |
| 55 | 2026-08-17 | instrument | **Two failure rates were described as agreement.** `rho_merge` 0.841 and `rho_split` 0.810 are the fractions of gold pairs *wrongly* unified and gold clusters *wrongly* split; the paper said they "correlate well with the reference", turning an 84% error rate into the reassuring half of a sentence. The genuine correlation (0.451) sat unreported in the same JSON. Same defect class as entry 53, found in a different file. |
| 56 | 2026-08-17 | process | **A fix introduced a worse problem than the one it solved.** Correcting the inversion polarity, I rewrote P1 to call the global figures "evidence against the precondition" and rescued it with an unregistered rank-2/rank-3 restriction. But the registered criterion is ρ > 0.9 → fail, and ρ is 0.34-0.48 at *every* granularity and rank map: it passes everywhere. The rewrite turned a clean pre-registered pass into something shaped like HARKing. Now reports the registered test as passing, with the granularity spread as the reason not to over-read it. |
| 57 | 2026-08-17 | instrument | **The pre-registered significance test pooled two corpora.** The primary cell is identified by four keys, and the E8.3 gold-subset runs share all four — so ASO saw n=20 for four models (two populations) against n=10 for the rest. Fixed at source and recomputed from the recorded runs; the deterministic dense baseline is excluded because a point mass has no dominance relation worth testing. The verdict is unchanged, which means the finding survives a valid test rather than depending on the flawed one. |

| 58 | 2026-08-17 | instrument | **A probe control turned a finding into a tautology.** E-NEW-3 probes narrator identity from rank-1 representations. Before quoting it I ran the same probe on the raw input features and got exactly **1.000**. Reading `build_features` explains it: a rank-k cell is the mean of its constituent rank-0 narrator features, and a rank-1 moment has exactly one narrator, so a moment's input vector IS its narrator's vector. Verified on the archive: max elementwise spread between two moments by the same narrator is `0.000e+00`. Without the control I would have written that the architectures *learn* narrator identity; they inherit it, and message passing only blurs it. |
| 59 | 2026-08-17 | inner-loop | **E-NEW-1 demonstrates the split mechanism.** Shuffling narrator labels (segments-per-narrator preserved) and re-running both splits: the complex's gain from a random split falls from **+0.2904** to **-0.0057** MAP, a reduction of 0.2961. The typed star, which never had the gain, does not lose one (-0.0331 to -0.1551). The comparison is made on the split *gap* rather than raw scores, because anonymisation changes which passages count as positives but transforms both splits identically. The paper's hedge ("the natural reading of the design rather than a demonstrated cause") is replaced by a section. |
| 60 | 2026-08-17 | instrument | **The E11 permutation test could never have fired.** Beyond beta_0 being constant at 1 across all 25 filtration levels, the shuffle itself reassigns member sets across rank-2 *cell IDs* — a relabelling. Homology ignores labels, so every permuted complex is isomorphic to the observed one and **both** nulls have sigma = 0. p = 1.0 was forced by construction and had been quoted as evidence. Re-ran on beta_1, which does vary (23246-25158): the registered kill test gives cross-validated R2 = 0.998 > 0.9, so the deletion verdict survives on evidence that is actually evidence. |
| 61 | 2026-08-17 | instrument | **E8.1's headline bounded a pipeline the paper does not use.** The kill criterion was evaluated at `primary_calibration = E3_5_automatic_extractor` (RBO 0.324, fails 0.70). The complex reported here is built from the archive's curated vocabulary, whose own calibration gives RBO **0.978** and passes. Both are now reported and scoped explicitly rather than switching the headline, which would have been a post-hoc rescue of a failed criterion. |
| 62 | 2026-08-17 | process | **Audited the registration against the code and found an undisclosed deviation.** `granularity_definitions` specifies `mid` and `fine` using a *chronology decade bin*; the implementation splits by geography and never bins by time. Densho segment records carry no date field, so the registered definition was not implementable. Filed as an amendment and disclosed in the paper body, with the scope narrowed: the sweep tests a topical/geographic relation, not temporal binning. |
| 63 | 2026-08-17 | outer-loop | **Round 3 paper work.** Added `Tasks and models` and `Related work` (three new verified citations, including the skeptical higher-order literature). Added Figure 7 for the mechanism. Softened the probe comparison after checking that M3/M5 seed spreads overlap — the decisive evidence is the anonymisation collapse, not the probe ordering. 79 tests pass; 134 macros, 0 missing; 10 direction claims hold; narrative diagnostics all pass except the documented negation-rate miss; 16 pages, no Type 3, 0 undefined refs, 0 overfull boxes. |
| 64 | 2026-08-18 | instrument | **The pre-registered interaction estimate pooled the whole grid.** E7.5 filtered `all_items` on model only, but `all_items` accumulates per-item records from every cell: coarse and fine granularities, all three splits, four negative regimes, two alternative rank maps and the gold subset. That is the source of the unexplained "343 events" against a complex with 141 rank-2 cells. Worse than a labelling slip: the pool **includes the random split**, the one condition where the ordering this coefficient describes is reversed, so the estimate mixed two populations that disagree about its sign. Same defect class as entry 57, third occurrence. Per-item records now carry their cell coordinates; refitted on the primary cell alone via `recompute_e7_5.py`. The effect is **four times larger** (-0.085, CI [-0.104, -0.065], p=2.5e-17, 75 test-set events) than the pooled -0.020. The claim survives and strengthens; the pooled number measured nothing the paper claims. |
| 65 | 2026-08-18 | instrument | **A second unsatisfiable condition, caught the same way as E11.** Testing the reviewer's suggested P2 reframe ("rank is reproducible exactly where the archive encodes it") I asked how often all three protocols defer to the manual's archive-practice tie-break. Answer: 0.0%, which looked like a clean negative until I checked the instrument. `a1_structural` sets (q2, q3) = (not has_children, has_children) and `a3_distributional` sets (s2>s3, s3>s2) — both strictly complementary, so neither can EVER be inconclusive. The condition was unsatisfiable by construction. The reframe is therefore not measurable on this design and was NOT added to the paper. |
| 66 | 2026-08-18 | outer-loop | **What the tie-break analysis did establish.** The manual specifies a tie-break that defers to the archive's facet depth — the one clause that would ground the ladder in archive practice rather than in the annotator. It is reachable by only one of three faithful implementations (lexical, 46.0% of terms; structural and distributional, 0%). The specification appears to settle hard cases by appeal to the archive; in practice each implementation settles them itself. Added to P2 as the procedural view of the same finding the low alpha gives from the outcome side. |
| 67 | 2026-08-18 | process | **Withdrew an unsupported directional claim in P2.** The paper argued that programmatic annotators "share no misreadings, so the agreement they achieve is if anything an upper bound on what people would achieve." That assumes misreading is the only source of human disagreement; two people who both know Minidoka is a camp agree regardless of the manual, while three separately written heuristics share no such background. The direction of the bias is not established, so the paper no longer claims one. P2's statement is restated in terms of faithful implementations of a manual, which is what was measured. |
| 68 | 2026-08-18 | process | **Reconciled the three counts a reviewer flagged as inconsistent.** 141 = rank-2 cells in the primary complex; 127 = rank-2 cells after removing the 21 contested terms; 343 = the erroneous pooled figure, now replaced by 75 test-set events with the error disclosed in the text. All three are now defined where they appear. |
| 69 | 2026-08-18 | inner-loop | **E-NEW-2 dose-response: the prediction FAILED, and it is reported as failed.** Predicted a positive slope of the complex's per-narrator advantage on log segments-per-narrator under the random split, and a flat slope under narrator-disjoint as control. Neither held. Random slope +0.0155, CI [-0.0095, 0.0404], p=0.225 (crosses zero). Narrator-disjoint slope **-0.169**, CI [-0.241, -0.097], p=3.9e-06 -- strongly negative, not flat: the complex falls further behind on prolific narrators exactly when it cannot have seen them. The two slopes differ by +0.185, which suggests the random split removes a size penalty rather than adding an advantage, but that comparison was not registered, has no interval, and needs a slope-by-split interaction I did not fit. Recorded as an open observation, NOT as support. The mechanism claim continues to rest on the anonymisation collapse, which is unambiguous. |
| 70 | 2026-08-18 | process | **Resisted the rescue.** The tempting move was to report the +0.185 slope difference as confirming the dose-response. That is the same manoeuvre as entry 56 -- a failed pre-registered test rescued by an unregistered comparison found after seeing the data. The paper states the prediction, states that neither half held, and labels the difference an open observation. |
| 71 | 2026-08-18 | outer-loop | **Writing quality measured against the CCFA prose checker.** `check_prose_quality.py` reported a hard ERROR: 45 em dashes against a limit of 3, plus a semicolon-density advisory. Forty-five parenthetical interruptions in a 7k-word paper is punctuation doing sentence structure's job. Fixed each by meaning rather than by substitution: em dashes 45 -> 0, semicolons 4.13 -> 1.77 per 1k. Headings became verdict-first ("P1 holds: rank is not cardinality"); the model table's not-applicable marker moved from `---` to `\textsc{n/a}`. The surviving `formulaic_structure` warning was TRACED, not obeyed: it matches "The first holds. The second and third fail." in the abstract, which names the three preconditions by ordinal. That is the argument, and the humanization policy forbids rewording accurate prose to satisfy a checker. |
| 72 | 2026-08-18 | outer-loop | **Narrative: the introduction did not carry the paper's own finding.** The title led with the split; the introduction called it "a fourth result" and never stated the mechanism, so a reader met the strongest claim as an afterthought and its cause not at all. Rewrote the introduction and conclusion to carry origin, preconditions, result and mechanism. Did NOT restructure the body: the reviewer's own N1 keeps the preconditions as the setup that earns the split result, and the pre-registration is organised around them, so moving them would make a registered study look reframed. |
| 73 | 2026-08-18 | inner-loop | **E-NEW-5 intervened on the cause, and REFUTED the account I had already written.** Chose this over the reviewer's E-NEW-4 (a second dataset), which would prove nothing: the mechanism is a property of `build_features`, so the same code on another archive reproduces it trivially. Gave every rank-1 cell its own passage embedding instead of its narrator's mean, everything else fixed. Manipulation check passed (13315/13315 rows replaced; within-narrator spread 0.000e+00 -> 2.793e-01). **The gap did not close**: M5 +0.3147 -> **+0.3852**, M3 -0.0242 -> -0.0184. Only rank-1 changed; the rank-0 narrator layer and its message paths remained, and that is what the random split feeds on. Anonymisation destroys that layer and does close the gap. |
| 74 | 2026-08-18 | process | **A written claim was corrected by its own experiment.** The introduction and conclusion had already asserted "a moment's input vector IS its narrator's vector, therefore the random split leaks". E-NEW-5 shows that chain is insufficient. Rewrote intro, conclusion, mechanism section and both figure captions to the supported claim: the leak lives in the narrator layer the ladder is built on, not in the moment vector. Added direction check `gap_item_specific > 0` (11 claims now, 0 violated). Without the intervention the paper would have shipped a causal story its own data contradicts. |
| 75 | 2026-08-18 | process | **E1 pre-commitment, written before the run.** Global feature construction confounds two mechanisms under a random split: a straddling narrator repeats an identity across the partition, and that narrator's vector aggregates test-side text. Split-first construction — a narrator's vector built from that narrator's training-side segments, falling back to their own segments when they have none — removes the second and leaves the first. **Prediction: the reversal persists.** It follows from two results already in this log: entry 73 replaced every rank-1 moment vector with its own passage embedding and the gap *widened* (+0.3147 to +0.3852), so the moment layer does not carry it; entry 59 destroyed narrator identity by anonymisation and the gap collapsed (+0.2904 to -0.0057), so the narrator layer does. E1 keeps the narrator layer intact and removes only the text aggregation. Committed readings: **persists** — identity repetition is the mechanism, and the direct test the paper says it needs has been run; **vanishes or shrinks sharply** — the mechanism is global owner-based lifting plus entity-overlapping evaluation, which is a different and more actionable finding, because feature-computation order is a bug fixable without changing the evaluation protocol. Both are reportable; neither is a disappointment. The fallback is not a leak: a new interview arrives at deployment with its full transcript, so a test narrator's own text is genuinely available, whereas a training narrator's test segments never were. |
